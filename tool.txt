gmail authenticationdbsa    elsd5ri2tflkdp6n
once authenticated, store the login data.
neededew gmail notification
new email sepration and auto repsonder.

create toools for gmail integration


two file for gmail 
1. authentication
2. notification
3. tools


similarly three tools for slack integration , google drive integration, .

gmail_oauth_base.py
slack_oauth_base.py


create webhook, delete webhook, update webhook

    def __init__(self, config: Config) -> None:
        super().__init__(config)
        # Override with Jira-specific env vars if provided
        self.client_id = config("JIRA_CLIENT_ID", default=self.client_id)
        self.client_secret = config("JIRA_CLIENT_SECRET", default=self.client_secret)



interation modeule.
tools
gmail 
notion.


lead capture,
support
cold call

